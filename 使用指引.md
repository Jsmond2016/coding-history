# 新设备使用指南

本文档将指导您在新设备上快速设置和使用 Coding History 代码提交记录统计系统。

## 📋 目录

- [环境要求](#环境要求)
- [安装步骤](#安装步骤)
- [配置说明](#配置说明)
- [启动服务](#启动服务)
- [初始化数据](#初始化数据)
- [验证安装](#验证安装)
- [常见问题](#常见问题)
- [数据库丢失或误删后的恢复](#数据库丢失或误删后的恢复)
- [日常使用](#日常使用)

---

## 🔧 环境要求

在开始之前，请确保您的设备已安装以下软件：

### 必需软件

1. **Node.js** (版本 >= 18.0.0)
   ```bash
   # 检查 Node.js 版本
   node --version
   ```

2. **pnpm** (推荐版本 >= 8.0.0)
   ```bash
   # 如果未安装，使用 npm 安装
   npm install -g pnpm
   
   # 检查 pnpm 版本
   pnpm --version
   ```

3. **Git** (用于访问本地 Git 仓库)
   ```bash
   # 检查 Git 版本
   git --version
   ```

### 可选工具

- **VS Code** 或您喜欢的代码编辑器
- **终端工具** (iTerm2, Terminal.app 等)

---

## 📦 安装步骤

### 1. 克隆或下载项目

```bash
# 如果项目在 Git 仓库中
git clone <repository-url>
cd coding-history

# 或者直接解压项目压缩包
cd coding-history
```

### 2. 安装项目依赖

```bash
# 在项目根目录执行（会自动安装 backend 和 frontend 的依赖）
pnpm install
```

**注意**：首次安装可能需要几分钟时间，请耐心等待。

### 3. 配置环境变量

创建后端环境变量文件：

```bash
cd backend
cp .env.example .env
```

编辑 `backend/.env` 文件，配置数据库路径和其他选项：

```env
# 数据库配置
# 重要：Prisma 中相对路径是相对于 backend/prisma/ 目录（schema 所在目录）解析的。
# 推荐：让数据库落在 backend/database/ 下，使用：
DATABASE_URL="file:../database/coding-history.db"

# 不要使用 file:./database/xxx.db，否则数据库会生成在 backend/prisma/database/ 中。
# 或使用绝对路径：
# DATABASE_URL="file:/Users/your-username/path/to/coding-history/backend/database/coding-history.db"

# 日志级别（可选）
LOG_LEVEL="info"

# 常用配置请直接参考 backend/.env.example
```

**环境变量说明**：
- `DATABASE_URL`: SQLite 数据库文件路径（必需）
- `LOG_LEVEL`: 日志级别，可选值：`debug`, `info`, `warn`, `error`（可选，默认 `info`）
- 项目启动后不会自动执行首次扫描
- 日常扫描依赖页面中的手动触发和自动定时任务
- 如需补历史数据，请手动运行 `pnpm init-scan`

**重要提示**：
- 数据库路径使用 `file:` 前缀。
- 本项目使用 **Prisma 7**：连接 URL 由 `backend/prisma.config.ts` 从环境变量 `DATABASE_URL` 读取；迁移与运行时均使用该配置。
- 推荐在 `.env` 中设置 `DATABASE_URL="file:./database/coding-history.db"`，数据库文件会落在 `backend/database/` 下（路径相对于 `backend/`）；或使用 `file:../database/coding-history.db` 将数据库放在项目根目录的 `database/` 下。
- 确保目标目录（如 `backend/database`）存在，若不存在需先 `mkdir` 或由迁移时创建。

### 4. 初始化数据库

```bash
cd backend

# 运行 Prisma 迁移，创建数据库表结构（Prisma 7 从 prisma.config.ts 读取 DATABASE_URL）
pnpm prisma migrate deploy

# 生成 Prisma Client（会输出到 backend/src/generated/prisma，安装依赖时 postinstall 会自动执行）
pnpm prisma generate
```

---

## ⚙️ 配置说明

### 1. 配置 Git 仓库

**注意**：配置已迁移到数据库，推荐使用前端配置管理页面进行配置。如果您有旧的配置文件，可以参考下面的格式进行迁移。

#### 方式一：使用前端配置管理页面（推荐）

1. 启动后端和前端服务
2. 访问前端页面，点击左侧菜单的"配置管理"
3. 在"仓库配置"标签页中：
   - 点击"添加仓库"按钮
   - 填写仓库信息（ID、名称、路径）
   - 配置作者信息

#### 方式二：从配置文件迁移（仅首次使用）

如果您有旧的 `backend/config/repositories.json` 配置文件，可以运行迁移脚本：

```bash
cd backend
pnpm migrate-config
```

#### 旧配置文件格式（仅用于迁移）

编辑 `backend/config/repositories.json` 文件，添加您要统计的 Git 仓库：

```json
{
  "repositories": [
    {
      "id": "my-project-1",
      "name": "我的项目 1",
      "path": "/absolute/path/to/your/repository-1",
      "enabled": true
    },
    {
      "id": "my-project-2",
      "name": "我的项目 2",
      "path": "/absolute/path/to/your/repository-2",
      "enabled": true
    }
  ],
  "authors": [
    {
      "name": "Your Name",
      "email": "your.email@example.com",
      "isDefault": true
    }
  ],
  "scanInterval": [
    {
      "cron": "0 10 * * *",
      "description": "每天上午10点"
    },
    {
      "cron": "0 19 * * *",
      "description": "每天傍晚19点"
    }
  ]
}
```

（旧版 JSON 若含 `ignoredBranches`，迁移时会被忽略，不再写入数据库。）

**配置说明**：

- `repositories`: 仓库列表
  - `id`: 唯一标识符（建议使用仓库名称的 kebab-case）
  - `name`: 显示名称
  - `path`: **绝对路径**（必须使用完整路径）
  - `enabled`: 是否启用（`true` 或 `false`）

- `authors`: 作者列表（用于过滤提交记录）
  - `name`: 作者名称
  - `email`: 作者邮箱（**必须与 Git 提交中的邮箱一致**）
  - `isDefault`: 是否为默认作者

- `scanInterval`: 定时扫描的 Cron 表达式配置
  - **数组格式**（推荐）：支持配置多个执行时间点
    ```json
    "scanInterval": [
      {
        "cron": "0 10 * * *",
        "description": "每天上午10点"
      },
      {
        "cron": "0 19 * * *",
        "description": "每天傍晚19点"
      }
    ]
    ```
    - `cron`: Cron 表达式（必需），格式：`分钟 小时 日 月 星期`
    - `description`: 描述信息（可选），用于日志输出
  - **字符串格式**（向后兼容）：单个 Cron 表达式
    ```json
    "scanInterval": "0 10 * * *"
    ```
  - Cron 表达式示例：
    - `"0 10 * * *"` - 每天上午10点
    - `"0 19 * * *"` - 每天傍晚19点
    - `"0 9,18 * * *"` - 每天上午9点和傍晚18点
    - `"0 0 * * 1"` - 每周一凌晨0点

**重要提示**：
- 仓库路径必须是**绝对路径**，不能使用相对路径
- 确保路径指向有效的 Git 仓库（包含 `.git` 目录）
- 确保应用有权限访问这些路径

### 2. 配置数据指标（可选）

在"配置管理"页面的"数据指标配置"标签页中，可以自定义：

- **提交次数阈值**：
  - 轻松阈值：提交次数 < 此值判定为轻松
  - 正常阈值：提交次数 >= 轻松阈值 且 < 此值判定为正常
  - 忙碌阈值：提交次数 >= 正常阈值 且 < 此值判定为忙碌
  - 疯狂阈值：提交次数 >= 忙碌阈值 且 < 此值判定为疯狂，>= 此值判定为超级疯狂

- **加班时间阈值**：提交时间 >= 此时间（小时）判定为加班（默认 19:00）

- **工作状态标签文本**：自定义每个工作状态的显示文本（如：轻松、正常、忙碌等）

- **工作状态标签颜色**：选择每个工作状态标签的颜色（支持 Ant Design 的所有颜色）

**默认配置**：
- 轻松阈值：6 次
- 正常阈值：10 次
- 忙碌阈值：15 次
- 疯狂阈值：20 次
- 加班时间：19:00

### 3. 验证仓库路径

在配置完成后，建议验证仓库路径是否正确：

```bash
# 检查仓库是否存在
ls -la /path/to/your/repository

# 检查是否为 Git 仓库
cd /path/to/your/repository && git status
```

---

## 🚀 启动服务

### 方式一：PM2 后台启动（推荐用于生产环境）

使用 PM2 在后台启动服务，不占用终端窗口，支持自动重启和日志管理：

```bash
# 启动服务（后台运行）
pnpm start

# 查看服务状态
pnpm status

# 查看日志
pnpm logs

# 停止服务
pnpm stop

# 重启服务
pnpm restart

# 删除进程
pnpm delete
```

**PM2 启动的优势：**
- ✅ 服务在后台运行，不占用终端窗口
- ✅ 自动重启（进程崩溃时）
- ✅ 日志管理（输出到文件）
- ✅ 进程监控和管理
- ✅ 可以关闭终端窗口，服务继续运行

### 方式二：分别启动（推荐用于开发）

**终端 1 - 启动后端服务**：

```bash
cd backend
pnpm dev
```

后端服务将在 `http://localhost:5188` 启动。

**终端 2 - 启动前端服务**：

```bash
cd frontend
pnpm dev
```

前端应用将在 `http://localhost:35173` 启动。

### 方式三：使用根目录脚本

在项目根目录，可以使用以下命令：

```bash
# 启动后端（在后台运行）
pnpm dev:backend &

# 启动前端
pnpm dev:frontend
```

### 方式四：使用启动脚本

如果项目中有 `start.sh` 脚本：

```bash
chmod +x start.sh
./start.sh
```

---

## 📊 初始化数据

### 首次使用 - 导入历史数据

在启动服务之前，建议先导入历史提交数据：

```bash
# 在项目根目录执行
pnpm init-scan

# 或扫描更多历史数据（例如 6 个月）
pnpm init-scan -- --months 6

# 或扫描 12 个月
pnpm init-scan -- --months 12
```

**说明**：
- 默认扫描最近 **3 个月** 的提交记录
- 扫描过程会显示进度和统计信息
- 支持断点续传，如果中断可以重新运行继续扫描
- 扫描完成后会自动去重，不会重复导入

**扫描过程示例**：

```
============================================================
[初始化扫描] 开始扫描最近 3 个月的提交记录（支持断点续传）...
============================================================
[初始化] 已同步 2 个仓库配置到数据库

[扫描仓库] 我的项目 1
  路径: /path/to/repository-1
  首次扫描: 从 2024-09-23 开始
  [分阶段扫描] 从 2024-09-23 继续，共 13 个阶段
  [阶段 1/13] 2024-09-23 ~ 2024-09-30
    ✓ 发现: 45 条, 新增: 45 条, 跳过: 0 条
    [进度更新] 已扫描到 2024-09-30
  ...

============================================================
[扫描完成] 统计信息:
  已完成仓库: 2 个
  扫描提交总数: 1046 条
  新增入库: 1046 条
  跳过重复: 0 条
============================================================
```

### 自动定时扫描

服务启动后，系统会自动在以下时间执行增量扫描：
- **每天上午 10:00**
- **每天傍晚 19:00**

每次扫描会自动同步最近一周的代码记录，确保数据及时更新。

---

## ✅ 验证安装

### 1. 检查服务状态

**后端健康检查**：

```bash
curl http://localhost:5188/health
```

应该返回：
```json
{"status":"ok"}
```

**前端访问**：

在浏览器中打开 `http://localhost:35173`，应该能看到应用界面。

### 2. 检查数据库

```bash
cd backend

# 使用 Prisma Studio 查看数据库（可选）
pnpm prisma studio
```

### 3. 检查日志

查看后端日志文件：

```bash
# 应用日志
tail -f backend/logs/app-$(date +%Y-%m-%d).log

# 错误日志
tail -f backend/logs/error-$(date +%Y-%m-%d).log
```

---

## 🔍 常见问题

### Q1: 安装依赖失败

**问题**：`pnpm install` 报错

**解决方案**：
```bash
# 清除缓存
pnpm store prune

# 删除 node_modules 和 lockfile，重新安装
rm -rf node_modules backend/node_modules frontend/node_modules
rm pnpm-lock.yaml
pnpm install
```

### Q2: 数据库连接失败 / 数据库文件被误删

**问题**：启动时提示数据库连接错误，或数据库文件不存在（例如误删了 `coding-history.db`）

**解决方案**：请直接按 [数据库丢失或误删后的恢复](#数据库丢失或误删后的恢复) 操作即可。

### Q3: 数据库文件生成在 backend/prisma/database/ 而不是 backend/database/

**原因**：Prisma 中 `DATABASE_URL` 的相对路径是相对于 **`backend/prisma/`**（schema 所在目录）解析的。若写成了 `file:./database/coding-history.db`，就会生成在 `backend/prisma/database/`。

**处理**：
1. 在 `backend/.env` 中改为推荐配置：`DATABASE_URL="file:../database/coding-history.db"`。
2. 若已有数据在 `backend/prisma/database/coding-history.db`，可移动后继续用：
   ```bash
   mkdir -p backend/database
   mv backend/prisma/database/coding-history.db backend/database/
   ```
3. 若不需要保留数据，删除 `backend/prisma/database/` 下的数据库文件，确保 `backend/database` 目录存在，再执行 `cd backend && pnpm prisma migrate deploy && pnpm prisma generate`。

### Q4: 仓库扫描失败

**问题**：扫描时提示仓库路径不存在或无法访问

**解决方案**：
1. 检查 `repositories.json` 中的路径是否为**绝对路径**
2. 验证路径是否存在：`ls -la /path/to/repository`
3. 检查路径权限：确保应用有读取权限
4. 确认路径指向有效的 Git 仓库（包含 `.git` 目录）

### Q5: 端口被占用

**问题**：启动时提示端口 5188 或 35173 已被占用

**解决方案**：
```bash
# 查找占用端口的进程
lsof -i :5188
lsof -i :35173

# 关闭进程（替换 PID 为实际进程 ID）
kill -9 <PID>

# 或使用项目提供的脚本（如果有）
./scripts/killport 5188
./scripts/killport 35173
```

### Q6: 定时任务不执行

**问题**：配置了定时任务但没有执行

**解决方案**：
1. 检查后端服务是否正常运行
2. 查看日志文件确认是否有错误
3. 检查 `repositories.json` 中的 `scanInterval` 配置是否正确
4. 确认系统时间是否正确

### Q7: 前端无法连接后端

**问题**：前端页面显示无法获取数据

**解决方案**：
1. 确认后端服务正在运行（`http://localhost:5188`）
2. 检查浏览器控制台是否有 CORS 错误
3. 检查 `backend/src/index.ts` 中的 CORS 配置
4. 确认防火墙没有阻止端口访问

### Q8: Prisma Client 未生成

**问题**：运行时提示找不到 Prisma Client

**解决方案**：
```bash
cd backend
pnpm prisma generate
```

---

## 🔄 数据库丢失或误删后的恢复

如果不小心删除了数据库文件（如 `coding-history.db`），按以下步骤可重新创建数据库并正常启动项目。

### 1. 确认环境变量

确保 `backend/.env` 存在且包含正确的 `DATABASE_URL`：

```bash
cd backend

# 若没有 .env，从示例复制
cp .env.example .env

# 查看当前配置（可选）
grep DATABASE_URL .env
```

`.env` 中典型配置示例：

```env
# 推荐：数据库在 backend/database/ 下（相对路径相对于 backend/prisma/ 解析）
DATABASE_URL="file:../database/coding-history.db"

# 不推荐：若写 file:./database/xxx.db，数据库会落在 backend/prisma/database/ 下
# DATABASE_URL="file:./database/coding-history.db"

# 或使用绝对路径
# DATABASE_URL="file:/Users/你的用户名/Desktop/MyCode/github/coding-history/backend/database/coding-history.db"
```

**说明**：Prisma 中相对路径是相对于 **`backend/prisma/`**（schema 所在目录）解析的。`file:` 前缀必须保留。

### 2. 创建数据库所在目录

根据你在 `.env` 里填的路径，创建对应目录（若不存在）。**相对路径是相对于 `backend/prisma/` 解析的**：

```bash
# 在项目根目录执行
cd /path/to/coding-history

# 推荐：DATABASE_URL 为 file:../database/coding-history.db → 数据库在 backend/database/
mkdir -p backend/database

# 若误用了 file:./database/coding-history.db → 数据库会在 backend/prisma/database/
# mkdir -p backend/prisma/database
```

### 3. 应用迁移并生成 Prisma Client

在 **backend** 目录下执行：

```bash
cd backend

# 应用所有迁移，会创建新的空数据库和表结构
pnpm prisma migrate deploy

# 生成 Prisma Client（必须）
pnpm prisma generate
```

执行成功后，会在你配置的路径下生成新的空数据库文件（如 `coding-history.db`）。

### 4. （可选）初始化数据指标配置

数据指标（阈值、标签等）可在前端「配置管理 → 数据指标配置」中设置，有默认值，一般无需额外脚本。

### 5. 重新配置与数据

- **仓库与作者**：在新库中需要重新在前端「配置管理」里添加仓库、作者等；若之前有 `backend/config/repositories.json`，可执行 `pnpm migrate-config` 导入。
- **历史提交数据**：新库是空的，需要重新做一次初始化扫描：
  ```bash
  # 在项目根目录
  pnpm init-scan
  # 或指定月数，例如 6 个月
  pnpm init-scan -- --months 6
  ```

### 6. 启动服务

按 [启动服务](#启动服务) 正常启动后端和前端即可。

**简要速查**（数据库误删后）：

```bash
cd backend
cp .env.example .env   # 没有 .env 时
# 编辑 .env：推荐 DATABASE_URL="file:../database/coding-history.db"（数据库在 backend/database/）
mkdir -p database      # 在 backend 下创建 database 目录（与 file:../database 对应）
pnpm prisma migrate deploy
pnpm prisma generate
cd ..
pnpm init-scan         # 需要历史数据时
# 然后按需启动后端/前端
```

---

## 📖 日常使用

### 启动项目

```bash
# 1. 启动后端（终端 1）
cd backend
pnpm dev

# 2. 启动前端（终端 2）
cd frontend
pnpm dev

# 3. 访问应用
open http://localhost:35173
```

### 手动触发扫描

在前端页面点击"手动扫描"按钮，或使用 API：

```bash
curl -X POST http://localhost:5188/api/v1/repositories/scan
```

### 查看统计数据

访问前端页面 `http://localhost:35173`，可以：
- 查看提交记录列表
- 按时间范围筛选
- 按仓库筛选
- 查看统计数据（提交次数、代码行数等）
- 查看工作状态报告

### 添加新仓库

1. 访问前端配置管理页面
2. 点击"添加仓库"按钮
3. 填写仓库信息（ID、名称、路径）
4. 配置作者信息
5. 保存后，在前端页面手动触发扫描，或等待定时任务执行

### 更新仓库配置

1. 访问前端配置管理页面
2. 在仓库列表中点击"编辑"按钮
3. 修改仓库信息或作者
4. 保存后配置立即生效，无需重启服务

### 配置数据指标

1. 访问前端配置管理页面
2. 切换到"数据指标配置"标签页
3. 修改阈值、标签文本或颜色
4. 点击"保存配置"后立即生效
5. 可以在"预览效果"区域查看配置效果

### 查看日志

```bash
# 实时查看应用日志
tail -f backend/logs/app-$(date +%Y-%m-%d).log

# 查看错误日志
tail -f backend/logs/error-$(date +%Y-%m-%d).log
```

### 备份数据

定期备份数据库文件：

```bash
# 备份数据库
cp backend/database/coding-history.db backend/database/coding-history.db.backup

# 或使用时间戳
cp backend/database/coding-history.db backend/database/coding-history.db.$(date +%Y%m%d)
```

---

## 🎯 快速检查清单

完成以下步骤后，项目应该可以正常运行：

- [ ] Node.js 和 pnpm 已安装
- [ ] 项目依赖已安装（`pnpm install`）
- [ ] `.env` 文件已创建并配置（`backend/.env`，含 `DATABASE_URL`）
- [ ] 数据库所在目录已创建（与 `.env` 中路径一致），数据库已初始化（`cd backend && pnpm prisma migrate deploy && pnpm prisma generate`）
- [ ] 仓库配置已完成（通过前端配置管理页面或迁移脚本）
- [ ] 数据指标配置已设置（可选，有默认值）
- [ ] 后端服务可以启动（`pnpm dev:backend`）
- [ ] 前端服务可以启动（`pnpm dev:frontend`）
- [ ] 可以访问前端页面（`http://localhost:35173`）
- [ ] 可以执行初始化扫描（`pnpm init-scan`）

**若数据库文件被误删**：按 [数据库丢失或误删后的恢复](#数据库丢失或误删后的恢复) 操作即可恢复空库并重新配置。

---

## 📚 相关文档

- [README.md](./README.md) - 项目概述
- [scripts/INIT_SCAN.md](./scripts/INIT_SCAN.md) - 初始化扫描详细说明
- [scripts/QUICKSTART.md](./scripts/QUICKSTART.md) - 快速开始指南

---

## 💡 提示

1. **首次使用**：建议先配置 1-2 个仓库测试，确认无误后再添加更多仓库
2. **大型仓库**：对于提交记录很多的仓库，首次扫描可能需要较长时间，请耐心等待
3. **定期备份**：建议定期备份数据库文件，避免数据丢失
4. **监控日志**：定期查看日志文件，及时发现和解决问题

---

**祝您使用愉快！** 🎉

如有问题，请查看日志文件或参考相关文档。
