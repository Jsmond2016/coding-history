{
  "name": "git-statistics-app",
  "version": "1.0.0",
  "private": true,
  "description": "Git commit statistics application",
  "scripts": {
    "dev:backend": "pnpm --filter git-statistics-backend dev",
    "dev:frontend": "pnpm --filter git-statistics-frontend dev",
    "build:backend": "pnpm --filter git-statistics-backend build",
    "build:frontend": "pnpm --filter git-statistics-frontend build"
  }
}

