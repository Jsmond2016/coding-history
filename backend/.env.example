# 数据库配置
# SQLite 数据库文件路径。注意：相对路径是相对于 backend/prisma/ 目录解析的（即 schema 所在目录）。
# 推荐：数据库放在 backend/database/ 下，使用：
DATABASE_URL="file:./database/coding-history.db"

# 日志级别（可选）
# 可选值: trace, debug, info, warn, error, fatal
LOG_LEVEL="info"

ENABLE_STARTUP_SCAN=false